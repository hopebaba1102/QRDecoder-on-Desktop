To build qrcode-desktop, you must have the following programs/libraries
installed on your system:
  - Java SDK 1.6+
  - Apache Ant

Steps to build qrcode-desktop using Apache Ant:
  1) Open a terminal and cd to this directory.
  2) Type "ant".
  3) If the build was successful, you can find the executable java archive in build/jar.

Running the jar:
  1) Open a terminal and cd to the directory containing the jar file.
  2) Type "java -jar <jarfile>" (replace <jarfile> with the name of the jar file)

